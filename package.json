{
  "name": "api-football-mcp",
  "version": "1.0.0",
  "description": "MCP server for API-Football providing comprehensive Premier League data access",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:contract": "vitest run tests/contract",
    "test:integration": "vitest run tests/integration",
    "test:unit": "vitest run tests/unit",
    "test:performance": "vitest run tests/performance",
    "test:offline": "vitest run tests/unit tests/contract tests/performance",
    "test:online": "vitest run tests/integration",
    "lint": "eslint src tests --fix",
    "lint:check": "eslint src tests",
    "check-types": "tsc --noEmit",
    "check-types:watch": "tsc --noEmit --watch",
    "type-coverage": "tsc --noEmit --listFiles | wc -l",
    "cli:api": "node dist/cli/api-client.js",
    "cli:cache": "node dist/cli/cache.js",
    "cli:tools": "node dist/cli/tools.js",
    "cli:server": "node dist/cli/server.js",
    "start": "node dist/server.js",
    "settle": "node scripts/settle.mjs",
    "report": "node scripts/matchday-report.mjs",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "api-football",
    "premier-league",
    "football",
    "soccer",
    "typescript"
  ],
  "author": {
    "name": "MarvDann",
    "url": "https://github.com/MarvDann"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MarvDann/api-football-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/MarvDann/api-football-mcp/issues"
  },
  "homepage": "https://github.com/MarvDann/api-football-mcp#readme",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pino": "^9.9.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@stylistic/eslint-plugin": "^2.0.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "eslint": "^9.0.0",
    "pino-pretty": "^13.1.1",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
