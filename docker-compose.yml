services:
  api-football-mcp:
    build:
      context: .
      dockerfile: Dockerfile

    restart: unless-stopped

    environment:
      API_FOOTBALL_KEY: ${API_FOOTBALL_KEY}
      MCP_PATH_TOKEN: ${MCP_PATH_TOKEN}
      NODE_ENV: production
      LOG_LEVEL: info

    command: >
      sh -c "npm install -g supergateway &&
      exec supergateway --stdio 'node dist/server.js'
      --outputTransport streamableHttp
      --port 3000
      --streamableHttpPath /mcp/$$MCP_PATH_TOKEN
      --cors"

    expose:
      - "3000"

    labels:
      - "traefik.enable=true"
      - "traefik.http.services.api-football-mcp.loadbalancer.server.port=3000"
